import { Link, useLocation } from 'react-router-dom';
export default function RegistrationSuccessPage() { const { state } = useLocation(); const role = state?.role || 'VitalFlow'; return <div className="flex min-h-screen items-center justify-center bg-blue-50 px-6 py-12"><div className="w-full max-w-xl rounded-2xl border bg-white p-8 text-center shadow-sm md:p-12"><div className="mx-auto flex h-24 w-24 items-center justify-center rounded-full bg-red-50 text-5xl">✓</div><h1 className="mt-7 text-3xl font-bold">Registration Successful</h1><p className="mx-auto mt-4 max-w-md leading-7 text-slate-600">Your {role.toLowerCase()} account has been created. Complete your profile to unlock all VitalFlow features.</p><div className="mt-8 flex flex-col gap-3 sm:flex-row sm:justify-center"><Link className="primary-button" to={dashboardFor(role)}>Go to Dashboard</Link><Link className="secondary-button" to="/">Return Home</Link></div></div></div>; }
function dashboardFor(role) { return role === 'Donor' ? '/donor/dashboard' : role === 'Hospital' ? '/hospital/dashboard' : '/blood-bank/dashboard'; }
